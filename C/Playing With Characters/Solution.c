/**  
* @author SANKALP SAXENA  
*/

#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() 
{

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */   
    char ch;
     char str[20];
      scanf("%[^\n]%*c", str);
    printf("%s\n", str);
    
    scanf("%[^\n]%*c", str);
    printf("%s\n", str);
     scanf("%[^\n]%*c", str);
    printf("%s", str);
    return 0;
}

